public class App {
    public static void main(String[] args) throws Exception {
        
        
        
        
        int number;

        if (number == 0) { // if number = 0, print 0 binary.
            // without this, 0 returns blank in else since it's all 0
System.out.println(number + " in binary is:");
System.out.print("0");
} else {
System.out.println(number + " in binary is:");
int i = 0; // int i outside of loop so it can be reused by the next loop to start from
for (; i < binary.length; i++) { // finds the first 1 in the array
 if (binary[i] != 0) // starting from first position, go up 1 at a time, if reach a 1, break
     break;
}


    }
}
